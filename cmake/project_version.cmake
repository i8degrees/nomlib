# version.cmake:jeff
#
# Engine version used to publish in build
#
# See also
# 1. Top-level CMakeLists.txt
#

# IMPORTANT(JEFF): This is the version info given to nomlib's project call
# and should always be updated every time we publish a new engine version by
# creating a release branch or tag the branch with the version.
set( VERSION_MAJOR 0 )
set( VERSION_MINOR 13 )
set( VERSION_PATCH 1 )
